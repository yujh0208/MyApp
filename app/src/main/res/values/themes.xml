<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyTest" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#F5FFFA</item> <!--타이틀바(앱 이름 색상)-->
        <item name="colorPrimaryVariant">@color/black</item> <!--상태바(시간, 배터 표시되는 곳 색상)-->
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="android:alertDialogTheme">@style/MyAlertDialogStyle</item>

    </style>
    <style name="MyAlertDialogStyle" parent="@style/Theme.AppCompat.Dialog.Alert">
        <item name="android:textColor">@color/black</item>
        <item name="android:typeface">monospace</item>
        <item name="android:background">@color/colorGray</item>
        <item name="android:fontFamily">@font/juhee</item>
    </style>
</resources>